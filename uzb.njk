---
title: Ҳамма постлар
layout: default.njk
pagination:
  data: collections.postsuz
  size: 10
  alias: posts
---

<section class="posts">
  <div class="posts-content">
    <ul class="post-list">
      {%- for post in posts | reverse%}
        <li class="post">
          <a class="post-title" href="{{ post.url }}">{{ post.data.title }}</a>
          
          {% if post.data.description %}
            <p class="post-description">{{ post.data.description }}</p>
          {% endif %}
          
          <div class="meta">
            <time class="post-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
          </div> 
        </li>
      {%- endfor %}
    </ul>
    
    <section class="pagination"> 
      {% if pagination.href.previous %}
        <a href="{{ pagination.href.previous }}">энг янги</a>
      {% endif %}

      {% if pagination.href.next %}
        <a href="{{ pagination.href.next }}">архив</a>
      {% endif %}
    </section>
  </div> 
</section>